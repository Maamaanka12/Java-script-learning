// items = ["Rice", "Sugar", "Flour", "Tea"]
// prices = [3, 5, 2, 4]
// 👉 Print each item and its price using a for loop or forEach().
let items = ["Rice", "Sugar", "Flour", "Tea"];
let prices = [3, 5, 2, 4];

// Using a for loop
for (let i = 0; i < items.length; i++) {
  console.log(`${items[i]}: $${prices[i]}`);
}