// Create an array destinations = ["Doha", "Dubai", "Nairobi", "Istanbul"].
// 👉 Use slice() to copy only the first two destinations into a new array.
const destinations = ["Doha", "Dubai", "Nairobi", "Istanbul"];
const firstTwoDestinations = destinations.slice(0, 2);
console.log(firstTwoDestinations);