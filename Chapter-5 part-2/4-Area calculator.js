//A builder wants a program to calculate the area of a rectangle. 
// Write a function expression that takes length and width as parameters and returns the area.
//  Print the result.
var calculateArea = function(length, width) {
    return length * width;
};
console.log("Area of rectangle: " + calculateArea(5, 3));